# Winget packages — Windows 10/11
# NOTE: scoop is the preferred path on this project (see scoop.txt / install.ps1).
# Win11 IoT LTSC often ships WITHOUT the App Installer (winget) and Store, so
# winget may be unavailable there — use scoop. This list is a fallback reference.
#
# Run from an elevated PowerShell prompt:
#   winget import --import-file winget.json
# Or install individually:
#   winget install <Id>

# Core editor
Neovim.Neovim

# Version control
Git.Git

# Search / file tools (used by telescope)
BurntSushi.ripgrep.MSVC
sharkdp.fd

# Language runtimes
GoLang.Go
OpenJS.NodeJS.LTS
Python.Python.3.12
Rustlang.Rustup           # installs rustup; run `rustup component add rust-analyzer rustfmt clippy` after

# Build tools (telescope-fzf-native needs make + a C compiler)
# Option A — LLVM/clang (lighter):
LLVM.LLVM
# Option B — Visual Studio Build Tools (heavier, also covers MSVC):
# Microsoft.VisualStudio.2022.BuildTools

# make (GNU make via GnuWin32 or choco; or use the VS devenv)
GnuWin32.Make

# tree-sitter CLI — REQUIRED: nvim-treesitter 'main' runs `tree-sitter build`.
# Not in winget; install via scoop (`scoop install tree-sitter`),
# `cargo install tree-sitter-cli`, or `npm i -g tree-sitter-cli`.

# Terminal / shell
Microsoft.WindowsTerminal
JanDeDobbeleer.OhMyPosh   # optional, for a styled prompt

# Fonts — JetBrains Mono Nerd Font
# Winget doesn't have font packages; install from:
#   https://www.nerdfonts.com/font-downloads
# Or via Scoop (see scoop.txt): scoop install nerd-fonts/JetBrainsMono-NF

# tmux — not natively available via winget; use WSL or Windows Terminal tabs instead

# ── Installed automatically by Mason inside neovim ──────────────────
# LSP servers:   lua-language-server, rust-analyzer (via rustup), gopls,
#                basedpyright, bash-language-server, yaml-language-server,
#                taplo, marksman, powershell-editor-services
# Formatters:    stylua, ruff, goimports, shfmt, prettier
